// import { Producer } from "node-rdkafka";

// const producer = new Producer({
//   "metadata.broker.list": "localhost:9092",
// });

// producer.produce("Like", null, Buffer.from("oi"), null, Date.now());
